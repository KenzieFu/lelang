import 'package:flutter/material.dart';

class Bidders extends StatelessWidget {
  const Bidders({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: const EdgeInsets.symmetric(vertical: 10),
      color: Colors.red,
      child: const Row(
        children: [Text("Hellop")],
      ),
    );
  }
}
